# 🚀 Руководство по деплою на Render.com

## ✅ Предварительная подготовка (Выполнено!)

- [x] Создана ветка `server-only` без Unity файлов
- [x] Настроен CORS с переменными окружения
- [x] Добавлена валидация обязательных env переменных
- [x] Создан .env.example с примерами
- [x] Размер репозитория оптимизирован: 890 MB

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка MongoDB

1. Перейдите на https://www.mongodb.com/cloud/atlas
2. Зарегистрируйтесь (бесплатный план M0)
3. Создайте кластер (выберите регион ближе к вашим игрокам)
4. **Database Access**: Создайте пользователя с правами Read/Write
5. **Network Access**: Добавьте `0.0.0.0/0` (разрешить все IP)
6. Нажмите **Connect** → **Connect your application**
7. Скопируйте connection string:
   ```
   mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/aetherion?retryWrites=true&w=majority
   ```
8. Замените `<username>` и `<password>` на ваши данные

### Шаг 2: Создание Web Service на Render

1. Перейдите на https://render.com
2. Зарегистрируйтесь через GitHub
3. Нажмите **New +** → **Web Service**
4. Выберите репозиторий: **LexMaXx/aetherion-server**
5. **⚠️ ВАЖНО:** Выберите ветку **`server-only`**

### Шаг 3: Настройка сервиса

**Name:** `aetherion-server` (или любое доступное имя)

**Region:** Europe (Frankfurt) или ближайший к игрокам

**Branch:** `server-only` ⚠️

**Root Directory:** (оставьте пустым)

**Runtime:** `Node`

**Build Command:** `npm install`

**Start Command:** `npm start`

**Instance Type:** `Free` (для начала, потом можно upgrade)

### Шаг 4: Environment Variables

Нажмите **Advanced** → **Add Environment Variable**

Добавьте следующие переменные:

| Key | Value | Пример |
|-----|-------|--------|
| `NODE_ENV` | `production` | `production` |
| `MONGODB_URI` | *Ваш connection string из Шага 1* | `mongodb+srv://user:pass@cluster0.xxxxx.mongodb.net/aetherion` |
| `JWT_SECRET` | *Случайная строка 32+ символов* | Сгенерируйте: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` |
| `ALLOWED_ORIGINS` | *Домены через запятую* | `https://yourgame.com` или пока оставьте пустым |

**Примечание:** `PORT` не нужен - Render установит автоматически

### Шаг 5: Deploy!

1. Нажмите **Create Web Service**
2. Render начнёт деплой (3-5 минут)
3. Следите за логами в реальном времени
4. Дождитесь сообщения: **"Your service is live 🎉"**

### Шаг 6: Проверка

1. Скопируйте URL сервиса (например: `https://aetherion-server.onrender.com`)
2. Откройте в браузере
3. Должны увидеть:
   ```json
   {
     "message": "Aetherion Server is running!",
     "version": "2.3.2-socket-debug",
     "status": "online"
   }
   ```

4. Проверьте логи на наличие ошибок

## 🔄 Обновление сервера

Когда вы меняете серверный код:

### Вариант 1: Изменения только в server-only ветке
```bash
git checkout server-only
# Внесите изменения
git add .
git commit -m "Update server"
git push origin server-only
```

### Вариант 2: Изменения из main ветки
```bash
git checkout main
# Внесите изменения в серверные файлы
git add .
git commit -m "Update server logic"
git push origin main

# Перенесите в server-only
git checkout server-only
git merge main
git push origin server-only
```

Render автоматически задеплоит изменения при push в `server-only`!

## 🎮 Подключение Unity клиента

В Unity установите URL сервера:

```csharp
string serverUrl = "https://aetherion-server.onrender.com";
```

**Для CORS:** После деплоя клиента обновите `ALLOWED_ORIGINS` на Render:
```
Environment Variables → Edit → ALLOWED_ORIGINS
Добавьте: https://your-unity-game.com
```

## ⚠️ Важные замечания

### Free Plan ограничения:
- ✅ 750 часов/месяц бесплатно (достаточно для одного сервиса 24/7)
- ⚠️ Спит после 15 минут неактивности (холодный старт ~30 сек)
- ⚠️ 512 MB RAM
- ⚠️ Shared CPU

### Как избежать засыпания (Paid Plan):
Upgrade до **Starter** ($7/месяц):
- Нет засыпания
- Больше RAM
- Dedicated CPU

### Мониторинг
- Dashboard → Metrics: CPU, Memory, Response Time
- Logs: Реальные логи сервера
- Events: История деплоев

## 🐛 Troubleshooting

### Ошибка: "Missing required environment variables"
**Решение:** Проверьте что добавили `MONGODB_URI` и `JWT_SECRET`

### Ошибка: "MongoServerError: bad auth"
**Решение:** Проверьте логин/пароль в `MONGODB_URI`

### Ошибка: "CORS error" в Unity
**Решение:** Добавьте домен клиента в `ALLOWED_ORIGINS`

### Сервер долго стартует
**Причина:** Free plan - холодный старт после сна
**Решение:** Upgrade до Starter или используйте keep-alive пинги

## 📊 Текущий статус

✅ Repository: https://github.com/LexMaXx/aetherion-server
✅ Branch: `server-only` (только серверный код)
✅ Size: 890 MB
✅ Files: 21 серверных файлов
✅ Security: CORS настроен, env vars валидируются
✅ Ready for deployment!

## 🆘 Нужна помощь?

- Render Docs: https://render.com/docs
- MongoDB Atlas Docs: https://docs.atlas.mongodb.com/
- Или спросите меня! 😊
